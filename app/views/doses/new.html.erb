<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient, collection: Ingredient.order(:name) %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
